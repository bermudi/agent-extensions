{
	"name": "vision",
	"version": "0.1.0",
	"description": "Query-driven vision tool for pi: ask a specific question about an image file, a vision model from pi's catalogue answers it. Does not touch the built-in read tool.",
	"license": "MIT",
	"type": "module",
	"private": true,
	"scripts": {
		"typecheck": "tsc --noEmit",
		"test": "bun test",
		"format": "prettier --write \"*.ts\""
	},
	"devDependencies": {
		"@earendil-works/pi-ai": "^0.85.1",
		"@earendil-works/pi-coding-agent": "^0.85.1",
		"@types/bun": "^1.3.3",
		"prettier": "^3.8.4",
		"typebox": "^1.3.7",
		"typescript": "^5.9.3"
	}
}

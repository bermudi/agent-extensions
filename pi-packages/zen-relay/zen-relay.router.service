[Unit]
Description=zen-relay router — round-robins pi's requests across gateway leaves
After=network-online.target

[Service]
ExecStart=%h/.local/bin/zen-relay router
EnvironmentFile=%h/.config/zen-relay/router.env
Restart=always
RestartSec=3

[Install]
WantedBy=default.target

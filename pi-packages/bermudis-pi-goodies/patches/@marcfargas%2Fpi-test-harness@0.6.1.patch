diff --git a/dist/session.js b/dist/session.js
index f7dc5de9be959c541ee4bab0ad1424656993bbe9..234a0adfd7fed6545738ce1825632ea7aa2fc2b8 100644
--- a/dist/session.js
+++ b/dist/session.js
@@ -12,7 +12,7 @@ import * as fs from "node:fs";
 import * as path from "node:path";
 import * as os from "node:os";
 import { createAgentSession, DefaultResourceLoader, SessionManager, SettingsManager, } from "@earendil-works/pi-coding-agent";
-import { getModel } from "@earendil-works/pi-ai";
+import { getModel } from "@earendil-works/pi-ai/compat";
 import { createPlaybookStreamFn } from "./playbook.js";
 import { interceptToolExecution } from "./mock-tools.js";
 import { createMockUIContext } from "./mock-ui.js";
@@ -50,6 +50,12 @@ export async function createTestSession(options = {}) {
     });
     // Override getApiKey to bypass real auth checks (on both agent and session)
     session.agent.getApiKey = async () => "test-key";
+    // Pi 0.84 moved model authentication behind ModelRuntime; the harness must
+    // bypass that check as well so model-switch tests stay offline.
+    const modelRuntime = session._modelRuntime;
+    if (modelRuntime) {
+        modelRuntime.checkAuth = async () => true;
+    }
     // The session also validates via _modelRegistry.getApiKey — patch it
     const origModelRegistry = session._modelRegistry;
     if (origModelRegistry) {
diff --git a/src/session.ts b/src/session.ts
index 83991343cb13c8297b7fc4cd04f7b910466f0edc..2e1086e4859499924ec2d3ad7edce7c0ddf5d7f9 100644
--- a/src/session.ts
+++ b/src/session.ts
@@ -20,7 +20,7 @@ import {
 	type AgentSessionEvent,
 } from "@earendil-works/pi-coding-agent";
 import type { AgentTool } from "@earendil-works/pi-agent-core";
-import { getModel } from "@earendil-works/pi-ai";
+import { getModel } from "@earendil-works/pi-ai/compat";
 import { createPlaybookStreamFn, type PlaybookState } from "./playbook.js";
 import { interceptToolExecution } from "./mock-tools.js";
 import { createMockUIContext } from "./mock-ui.js";
@@ -70,6 +70,12 @@ export async function createTestSession(options: TestSessionOptions = {}): Promi
 
 	// Override getApiKey to bypass real auth checks (on both agent and session)
 	(session.agent as any).getApiKey = async () => "test-key";
+	// Pi 0.84 moved model authentication behind ModelRuntime; the harness must
+	// bypass that check as well so model-switch tests stay offline.
+	const modelRuntime = (session as any)._modelRuntime;
+	if (modelRuntime) {
+		modelRuntime.checkAuth = async () => true;
+	}
 	// The session also validates via _modelRegistry.getApiKey — patch it
 	const origModelRegistry = (session as any)._modelRegistry;
 	if (origModelRegistry) {
